const { findLargestNumberInArray } = require("./easyAndMedium");
// harder

// check if anagram, function isAnagram(string1, string2) {}
function isAnagram(string1, string2) {
  if (string1 === string2) console.log("Some string");
}

// sort a list in place, function sort(numbers) {}
function sort(numbers) {
  let a = numbers;
  console.log("Some string");
}

console.log(findLargestNumberInArray([1, 2, 3, 4, 5, 6, 7, 8]));
export { isAnagram, sort };
